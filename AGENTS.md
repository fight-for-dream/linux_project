# AGENTS.md

## 适用范围

本文件适用于整个 `hcm_project` 仓库。开始工作前先阅读根目录的
`PROJECT_CONTEXT.md`；涉及驱动、设备树、LVGL、MQTT、构建、部署或板端
调试时，使用仓库 Skill：`imx6ull-hcm-development`。

## 项目边界

- 硬件平台是百问网 i.MX6ULL_PRO，目标内核是 Linux 4.9.88。
- Windows PC 保存和管理源码；Ubuntu 虚拟机负责 ARM 交叉编译、完整设备树
  合并和 DTB 构建；开发板负责加载模块和硬件验证。
- 本仓库只保存自主源码、设备树片段、配置和说明。完整 Linux、LVGL、
  mqttclient、工具链以及编译产物位于仓库外。
- 编号目录是开发过程的阶段快照。用户未指定覆盖旧阶段时，保留已验证目录，
  新功能从合适基线建立新的编号目录。
- 仓库中功能最完整的源码是 `20_mqtt_connection_status`；
  `21_system_autostart` 当前仍启动阶段 19，不能静默改成阶段 20。

## 代码和设计习惯

- 驱动和 C 程序尽量保持韦东山教学代码的直接、清晰风格，沿用仓库现有的
  缩进、大括号和命名习惯，避免无必要的框架与抽象层。
- 字符设备沿用项目现有的传统字符设备写法；除非用户明确要求，不改用
  `miscdevice`。
- 只使用 Linux 4.9.88 可用的内核 API。接口不确定时，以本项目对应内核源码
  为准，不按新内核文档猜测。
- 保持内核与用户空间结构体 ABI 一致；错误使用标准负 errno 返回，应用使用
  `errno`/`perror` 说明错误。
- 同步、清理、解绑等机制只在正确性需要时加入，并用简短注释说明原因。
- DHT11 是时序敏感设备，不随意改变采样间隔、临界区或 GPIO 访问方式。
- 修改设备树前同时核对原理图、模块手册、现有 DTS/DTSI、pinctrl 和已经占用
  的 GPIO/I2C/SPI 资源，保证四个驱动仍可同时工作。
- 阶段 10 之后的 LVGL 应用在 Ubuntu 的外部 LVGL 工程中编译；不要为这些
  阶段凭空补一个未经验证的独立 Makefile。
- 普通驱动/应用 Makefile 继续参考 `general_Makefile` 和现有阶段，除非目标
  构建环境要求不同。

## 工作流程

1. 先确认目标阶段、预期行为和完成标准，再搜索相关调用方、数据结构、设备
   节点、compatible、MQTT Topic 和配置格式。
2. 区分三类信息：源码可确认、用户/板端输出已验证、尚待确认。不要把推测写成
   已验证事实。
3. 硬件或接口变更先做资源冲突检查，再实施最小且可解释的修改。
4. 命令按 `Windows PowerShell`、`Ubuntu`、`开发板 shell` 分组，给出可直接
   复制的版本。
5. 当前会话不能访问 Ubuntu 或开发板时，可以完成源码与命令，但必须明确列出
   尚未执行的交叉编译和板端验证。
6. 新确认的稳定环境事实应同步更新 `PROJECT_CONTEXT.md`，不得记录密码、
   Token、SSH 私钥或热点密码。

## 验证与完成标准

- 至少检查完整 diff，并运行 `git diff --check`。
- 不提交 `.o`、`.ko`、DTB、静态/动态库、ARM 可执行文件、日志、CSV、完整
  第三方源码或凭据。
- 驱动变更需说明 Ubuntu 编译、模块加载、`dmesg`、设备节点、单模块测试和
  四模块共同加载的验证方法。
- 综合应用变更需覆盖正常采集、传感器错误、线程退出、EEPROM 配置、LVGL
  交互以及相关 MQTT 行为。
- 电机测试应限制步数和运行时间，并确保机构处于安全状态。
- 只有看到实际命令结果后才能写“编译通过”或“开发板测试通过”。
- 最终说明应包含：修改文件、设计理由、运行逻辑、已执行验证、未验证项及
  下一步板端命令。

## 部署约束

- 默认不修改开发板网络配置；只有用户明确提出时才处理网络连接。
- `21_system_autostart` 只负责加载四个驱动和启动应用，停止操作不卸载模块。
- `BROKER_HOST`、板端目录和设备 IP 都是部署参数，热点或目录变化后需要重新
  确认。
